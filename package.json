{
  "name": "summaryplus",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "license": "AGPL-3.0-only",
  "repository": "https://github.com/Tantoofaaz777/SummaryPlus",
  "scripts": {
    "build": "bun build src/backend.ts --outfile=dist/backend.js --target=bun && bun build src/frontend.ts --outfile=dist/frontend.js --target=browser",
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "devDependencies": {
    "bun-types": "^1.3.14",
    "lumiverse-spindle-types": "0.6.12",
    "typescript": "^6.0.2"
  }
}
